# Dependencias solo para desarrollo y tests (no van en la imagen de producción).
# Instalar con: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<0.25.0
